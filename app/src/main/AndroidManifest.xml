<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.weblogestaan.sardabir" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:configChanges="orientation|screenSize"
        android:screenOrientation="portrait"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
        <activity
            android:logo="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".MainActivity">
        </activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".LoginActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".SubjectsActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".TermActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".LinkActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".RenewsActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".HotActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".NotificationsActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".ProfileActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".FriendsActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".RegisterActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".SuggestActivity"></activity>

        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".RessetpassActivity"></activity>

        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".GuestActivity"></activity>

        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".EditprofileActivity"></activity>
        <activity
            android:logo="@mipmap/ic_launcher"
            android:name=".UsersActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"></uses-permission>

</manifest>
